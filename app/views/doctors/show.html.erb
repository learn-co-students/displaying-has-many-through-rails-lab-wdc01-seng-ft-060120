<h1><%= @doctor.name %></h1>

<%= "Department: #{@doctor.department}" %>

<h2>Appointments:</h2>
<ul>
<% @doctor.appointments.each do |apt| %>
    <li><%= apt.appointment_datetime.strftime("%B %e, %Y at %k:%M") %><br>
    <%= link_to apt.patient.name, patient_path(apt.patient) %>
<% end %>
</ul>